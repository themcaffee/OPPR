name: Publish to npm

on:
  release:
    types: [published]

permissions:
  contents: read
  id-token: write # Required for npm provenance

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Full git history for provenance

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Extract version from tag
        id: extract_version
        run: |
          # Extract version from tag (e.g., v1.2.3 -> 1.2.3)
          TAG_NAME="${{ github.event.release.tag_name }}"
          VERSION="${TAG_NAME#v}"

          # Validate version format (basic semver check)
          if ! [[ "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+(-.*)?$ ]]; then
            echo "Error: Invalid version format '$VERSION'. Expected semver format (e.g., 1.2.3 or 1.2.3-beta.1)"
            exit 1
          fi

          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Extracted version: $VERSION"

      - name: Update package.json version
        working-directory: packages/core
        run: |
          pnpm version ${{ steps.extract_version.outputs.version }} --no-git-tag-version
          echo "Updated package.json to version ${{ steps.extract_version.outputs.version }}"

      - name: Run quality checks and build
        working-directory: packages/core
        run: pnpm run prepublishOnly

      - name: Dry-run publish
        working-directory: packages/core
        run: |
          echo "Running pnpm publish --dry-run to validate package..."
          pnpm publish --dry-run --no-git-checks
          echo "Dry-run completed successfully!"

      - name: Publish to npm with provenance
        working-directory: packages/core
        run: pnpm publish --provenance --access public --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_AUTH_TOKEN }}

      - name: Report success
        run: |
          echo "✅ Successfully published @oppr/core@${{ steps.extract_version.outputs.version }} to npm!"
          echo "Package URL: https://www.npmjs.com/package/@oppr/core/v/${{ steps.extract_version.outputs.version }}"
