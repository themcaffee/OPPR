// Public types for client configuration and responses
export type {
  MatchplayClientOptions,
  TournamentListParams,
  GameListParams,
  UserOptions,
  RatingParams,
  TransformOptions,
  PlayerTransformOptions,
  TournamentGame,
  TournamentRound,
  TournamentStats,
} from './client-options.js';

// Internal API response types (not re-exported from main index)
// These are only used internally by the client and transformers
